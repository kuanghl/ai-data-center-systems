import{_ as n,C as r,o as t,c as s,a0 as a,b as d,w as i,a as l,E as h,a1 as c}from"./chunks/framework.CzVS_xMp.js";const k=JSON.parse('{"title":"Domain-Specific Architectures for AI Inference","description":"","frontmatter":{},"headers":[],"relativePath":"inference/efficient-llm-inference-systems/appendix/hardware-architectures/domain-specific-architectures-for-ai-inference.ko.md","filePath":"inference/efficient-llm-inference-systems/appendix/hardware-architectures/domain-specific-architectures-for-ai-inference.ko.md","lastUpdated":1788432796000}'),p={name:"inference/efficient-llm-inference-systems/appendix/hardware-architectures/domain-specific-architectures-for-ai-inference.ko.md"};function m(u,e,g,f,b,y){const o=r("Mermaid");return t(),s("div",null,[e[1]||(e[1]=a(`<h1 id="domain-specific-architectures-for-ai-inference" tabindex="-1">Domain-Specific Architectures for AI Inference <a class="header-anchor" href="#domain-specific-architectures-for-ai-inference" aria-label="Permalink to &quot;Domain-Specific Architectures for AI Inference&quot;">​</a></h1><blockquote><p>Source: <a href="https://fleetwood.dev/posts/domain-specific-architectures" target="_blank" rel="noreferrer">Domain specific architectures for AI inference</a>, published 2025-08-03.</p><p>This is a Korean lecture-note adaptation, not a line-by-line full translation. The goal is to preserve the article&#39;s main argument and connect it to this repository&#39;s inference performance measurements.</p></blockquote><h2 id="reading-map" tabindex="-1">Reading Map <a class="header-anchor" href="#reading-map" aria-label="Permalink to &quot;Reading Map&quot;">​</a></h2><p>The core question of this note is simple.</p><blockquote><p>If you redesigned an accelerator for Transformer inference only, what structure would you need that differs from a GPU?</p></blockquote><p>To answer this question, the author first analyzes the bottlenecks of Transformer inference and derives hardware design principles from there. The conclusion is as follows.</p><ol><li>Hardware must directly support low-precision data types.</li><li>Memory transfer must be designed asynchronously from the start.</li><li>Dedicated hardware for tensor-aware memory transfer is needed.</li><li>A large scratchpad is more advantageous than an ordinary cache hierarchy.</li><li>On a single accelerator, memory bandwidth is extremely important.</li><li>Scale-out must be considered from the start.</li><li>Communication-dedicated hardware must complement the compute hardware.</li></ol><p>This list ties together Week 2&#39;s GPU memory hierarchy, Week 3&#39;s KV cache, and Week 4&#39;s quantization in a single viewpoint.</p><h2 id="source-structure" tabindex="-1">Source Structure <a class="header-anchor" href="#source-structure" aria-label="Permalink to &quot;Source Structure&quot;">​</a></h2><p>The original is a long essay, but read as a lecture note it has the following flow.</p><ol><li>It sets the optimization goals of AI inference as energy and capital efficiency.</li><li>It reduces Transformer inference to a memory movement problem.</li><li>It derives single-chip design principles such as lower precision, async transfer, DMA/TMA, and scratchpad.</li><li>It re-analyzes prefill/decode, matmul, attention, and KV cache from the arithmetic intensity perspective.</li><li>It extends beyond a single accelerator to model parallelism, MoE, AllToAll, and communication hardware.</li><li>It reasons about how test-time compute scaling will change hardware design targets.</li><li>It reads DSA cases such as TPU, Tenstorrent, Groq, and Cerebras against the design principles.</li></ol><p>So this note is less an &quot;introduction of a specific accelerator&quot; and more a <strong>note that back-derives hardware design constraints from the inference workload</strong>.</p><h2 id="_1-in-ai-inference-memory-comes-before-compute" tabindex="-1">1. In AI Inference, Memory Comes Before Compute <a class="header-anchor" href="#_1-in-ai-inference-memory-comes-before-compute" aria-label="Permalink to &quot;1. In AI Inference, Memory Comes Before Compute&quot;">​</a></h2><p>The note views the cost of AI inference in terms of energy and capital. In a world where many users keep calling models for long periods, raising FLOPS alone is not enough. The place where a system actually burns money is usually data movement.</p><p>Simplified, the Transformer forward pass repeats the following pattern.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Read a large weight tensor from HBM/DRAM.</span></span>
<span class="line"><span>Move it to a nearby memory or compute unit.</span></span>
<span class="line"><span>Multiply it with the activation.</span></span>
<span class="line"><span>Store the intermediate result again.</span></span>
<span class="line"><span>Repeat in the next layer.</span></span></code></pre></div><p>In the decode phase, the batch is small and few tokens are generated at a time. So it is closer to GEMV than GEMM, and the bytes read for weights dominate performance. This is the same thing Week 2 and Week 4 of this repository keep emphasizing.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>decode latency ~= model weight bytes / effective memory bandwidth + overhead</span></span></code></pre></div><p>So the first design goal of an inference accelerator is not &quot;more compute units&quot; but &quot;removing unnecessary data movement.&quot;</p><h3 id="_1-1-why-look-at-memory-cost-first" tabindex="-1">1.1 Why Look at Memory Cost First <a class="header-anchor" href="#_1-1-why-look-at-memory-cost-first" aria-label="Permalink to &quot;1.1 Why Look at Memory Cost First&quot;">​</a></h3><p>A common mistake in AI accelerator discussion is putting peak FLOPS at the center. But what matters in inference is not &quot;how many operations can be done&quot; but &quot;how fast and with how little energy can the values needed for the computation be brought in.&quot;</p><p>In decode in particular, producing one token reads the weights of each layer almost once. With a small batch, the degree of reusing the same weights across many tokens is low. So no matter how fast the Tensor Core is, if the weights cannot be brought in from HBM, it sits idle.</p><p>A simple example is the following.</p><table tabindex="0"><thead><tr><th>Model</th><th style="text-align:right;">Precision</th><th style="text-align:right;">Weight bytes</th><th style="text-align:right;">Minimum time to read from 3 TB/s HBM</th></tr></thead><tbody><tr><td>7B</td><td style="text-align:right;">BF16</td><td style="text-align:right;">14 GB</td><td style="text-align:right;">4.7 ms</td></tr><tr><td>70B</td><td style="text-align:right;">BF16</td><td style="text-align:right;">140 GB</td><td style="text-align:right;">46.7 ms</td></tr><tr><td>70B</td><td style="text-align:right;">INT4</td><td style="text-align:right;">35 GB</td><td style="text-align:right;">11.7 ms</td></tr></tbody></table><p>These values are theoretical lower bounds. In a real system, kernel launch, dequantization, KV cache read, sampling, scheduler, network, and framework overhead are added on top. Still, computing the lower bound tells you quickly what is impossible.</p><h3 id="_1-2-prefill-and-decode-through-arithmetic-intensity" tabindex="-1">1.2 Prefill and Decode Through Arithmetic Intensity <a class="header-anchor" href="#_1-2-prefill-and-decode-through-arithmetic-intensity" aria-label="Permalink to &quot;1.2 Prefill and Decode Through Arithmetic Intensity&quot;">​</a></h3><p>The original compares arithmetic intensity, i.e. <code>operations / bytes moved</code>, against the hardware ratio. Put simply, it is the following.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>algorithm intensity &gt; hardware ops:byte ratio  -&gt; possibly compute-bound</span></span>
<span class="line"><span>algorithm intensity &lt; hardware ops:byte ratio  -&gt; possibly memory-bound</span></span></code></pre></div><p>Prefill processes the prompt sequence in parallel. It has many large matrix multiplications, and the same weight tile can be reused across many token activations. So with a sufficiently long prompt and batch, it approaches compute-bound.</p><p>Decode produces one next token at a time. With a small batch, it reads many weights while the activation vector is small and the amount of computation is small. So it easily becomes memory-bound.</p><table tabindex="0"><thead><tr><th>Phase</th><th>Typical shape</th><th>Reuse</th><th>First-order bottleneck</th></tr></thead><tbody><tr><td>Prefill</td><td>GEMM</td><td>high</td><td>Tensor Core compute or HBM tile supply</td></tr><tr><td>Decode</td><td>GEMV-like</td><td>low</td><td>HBM bandwidth, KV cache bandwidth, launch overhead</td></tr><tr><td>Batched decode</td><td>small/medium GEMM</td><td>medium</td><td>HBM or compute depending on batch size</td></tr></tbody></table><p>In the Week 2 lab, the fact that the cuBLAS path switched from GEMV to Tensor Core GEMM as the batch grew is the same phenomenon. The algorithm did not change; the shape grew large enough to feed the hardware.</p>`,32)),(t(),d(c,null,{default:i(()=>[h(o,{id:"mermaid-283",class:"mermaid",graph:"%25%25%7Binit%3A%20%7B%22theme%22%3A%20%22base%22%2C%20%22themeVariables%22%3A%20%7B%22background%22%3A%20%22%23171717%22%2C%20%22primaryColor%22%3A%20%22%23232323%22%2C%20%22primaryTextColor%22%3A%20%22%23f5f5f5%22%2C%20%22primaryBorderColor%22%3A%20%22%23d0d0d0%22%2C%20%22lineColor%22%3A%20%22%23cfcfcf%22%2C%20%22fontFamily%22%3A%20%22Inter%2C%20Arial%2C%20sans-serif%22%7D%7D%7D%25%25%0Aflowchart%20LR%0A%20%20%20%20A%5BSmall%20batch%20decode%5D%20--%3E%20B%5BGEMV-like%20work%5D%0A%20%20%20%20B%20--%3E%20C%5BLow%20arithmetic%20intensity%5D%0A%20%20%20%20C%20--%3E%20D%5BHBM%20%2F%20overhead%20bound%5D%0A%20%20%20%20E%5BLong%20prefill%5D%20--%3E%20F%5BLarge%20GEMM%5D%0A%20%20%20%20F%20--%3E%20G%5BHigh%20arithmetic%20intensity%5D%0A%20%20%20%20G%20--%3E%20H%5BTensor%20Core%20bound%5D%0A%0A%20%20%20%20classDef%20primary%20fill%3A%23232323%2Cstroke%3A%23d0d0d0%2Ccolor%3A%23f5f5f5%2Cstroke-width%3A2px%3B%0A%20%20%20%20classDef%20secondary%20fill%3A%233b2f20%2Cstroke%3A%23d0d0d0%2Ccolor%3A%23f5f5f5%2Cstroke-width%3A2px%3B%0A%20%20%20%20classDef%20note%20fill%3A%2352676b%2Cstroke%3A%23d0d0d0%2Ccolor%3A%23f5f5f5%2Cstroke-width%3A2px%3B%0A%20%20%20%20classDef%20accent%20fill%3A%2362164d%2Cstroke%3A%23d0d0d0%2Ccolor%3A%23f5f5f5%2Cstroke-width%3A2px%3B%0A%20%20%20%20class%20A%2CE%20primary%0A%20%20%20%20class%20B%2CF%20secondary%0A%20%20%20%20class%20C%2CG%20note%0A%20%20%20%20class%20D%2CH%20accent%0A"})]),fallback:i(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=a(`<h2 id="_2-lower-precision-quantization-is-not-just-model-compression" tabindex="-1">2. Lower Precision: Quantization Is Not Just Model Compression <a class="header-anchor" href="#_2-lower-precision-quantization-is-not-just-model-compression" aria-label="Permalink to &quot;2. Lower Precision: Quantization Is Not Just Model Compression&quot;">​</a></h2><p>Lower precision produces three effects at once.</p><table tabindex="0"><thead><tr><th>Effect</th><th>Meaning for inference</th></tr></thead><tbody><tr><td>Capacity</td><td>A larger model or a longer KV cache fits in the same HBM.</td></tr><tr><td>Bandwidth</td><td>More weights/tokens can be read at the same bandwidth.</td></tr><tr><td>Silicon area</td><td>Smaller multipliers fit more in the same die area, or the area can be redirected to SRAM.</td></tr></tbody></table><p>Rephrased in the Week 4 viewpoint, it is the following.</p><table tabindex="0"><thead><tr><th>Format</th><th>Meaning in decode</th><th>Meaning in prefill</th></tr></thead><tbody><tr><td>BF16/FP16</td><td>weight bytes are large.</td><td>A stable baseline.</td></tr><tr><td>FP8/INT8</td><td>Can reduce both bandwidth and compute.</td><td>The Tensor Core path can get faster.</td></tr><tr><td>INT4/W4A16</td><td>Greatly reduces decode weight traffic.</td><td>The dequantization path can become the bottleneck.</td></tr><tr><td>FP4/NVFP4</td><td>The core frontier after Blackwell.</td><td>Whether hardware supports it matters.</td></tr></tbody></table><p>The important interpretation is this.</p><blockquote><p>Quantization is both a &quot;technology for making things fit in memory&quot; and a &quot;technology for reducing the bytes that must be read from HBM.&quot;</p></blockquote><h3 id="_2-1-lowering-precision-also-changes-the-silicon" tabindex="-1">2.1 Lowering Precision Also Changes the Silicon <a class="header-anchor" href="#_2-1-lowering-precision-also-changes-the-silicon" aria-label="Permalink to &quot;2.1 Lowering Precision Also Changes the Silicon&quot;">​</a></h3><p>An important point in the original is that precision reduction is not merely a memory optimization. As bit width shrinks, the circuit area and energy cost of the multiplier drop sharply. So the hardware architect has two choices.</p><ol><li>Pack more compute units into the same die area.</li><li>Reduce some compute units and give that area to SRAM/scratchpad, DMA, and communication engines.</li></ol><p>GPUs evolved toward keeping many compute units because of generality and ecosystem. In contrast, a DSA, if the target workload is clear, can invest area more aggressively in SRAM, data movement engines, and low-precision datapaths.</p><h3 id="_2-2-precision-has-a-floor" tabindex="-1">2.2 Precision Has a Floor <a class="header-anchor" href="#_2-2-precision-has-a-floor" aria-label="Permalink to &quot;2.2 Precision Has a Floor&quot;">​</a></h3><p>A lower bit width is not always better. LLM weights and activations carry information, and too low a precision harms model quality. So the important question for a DSA is not &quot;what is the lowest precision&quot; but &quot;what is the lowest precision and format that still satisfies the target quality.&quot;</p><table tabindex="0"><thead><tr><th>Question</th><th>Example</th></tr></thead><tbody><tr><td>Can only the weights be lowered?</td><td>W4A16, AWQ, GPTQ</td></tr><tr><td>Can the activations also be lowered?</td><td>W8A8, SmoothQuant, FP8</td></tr><tr><td>Is the accumulator precision sufficient?</td><td>INT8 multiply + FP16/FP32 accumulate</td></tr><tr><td>Does the format handle outliers?</td><td>FP8 E4M3 vs INT8 scale</td></tr><tr><td>Is the kernel actually fast?</td><td>fused Marlin/AWQ vs slow dequant path</td></tr></tbody></table><p>As the Week 4 lab concluded, reducing the bit width alone does not automatically reduce latency. Without a fused low-bit kernel and hardware support, the dequantization overhead eats the gain.</p><h2 id="_3-first-class-asynchronicity" tabindex="-1">3. First-Class Asynchronicity <a class="header-anchor" href="#_3-first-class-asynchronicity" aria-label="Permalink to &quot;3. First-Class Asynchronicity&quot;">​</a></h2><p>The accelerator must be designed so that compute units do not sit idle while waiting for memory transfers. For this, double buffering, pipelining, prefetching, and overlap are needed.</p><p>A simple structure is the following.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>buffer A: computing</span></span>
<span class="line"><span>buffer B: fetching the next tile from memory</span></span>
<span class="line"><span>swap</span></span>
<span class="line"><span>buffer B: computing</span></span>
<span class="line"><span>buffer A: fetching the next tile</span></span></code></pre></div><p>CUDA kernels, the Tensor Memory Accelerator, the TPU VMEM pipeline, and NCCL overlap are all ways of solving the same problem at different layers.</p><p>When drawing the roofline in the Week 2 lab, there are cases where the arithmetic intensity is sufficient yet the peak is not reached. In that case, the cause may not be a simple bandwidth shortage but a failure to overlap transfer and compute.</p><h3 id="_3-1-view-async-as-the-base-structure-not-a-feature" tabindex="-1">3.1 View Async as the Base Structure, Not a Feature <a class="header-anchor" href="#_3-1-view-async-as-the-base-structure-not-a-feature" aria-label="Permalink to &quot;3.1 View Async as the Base Structure, Not a Feature&quot;">​</a></h3><p>Asynchronicity is not a single library optimization. The hardware must be able to answer the following questions from the start.</p><table tabindex="0"><thead><tr><th>Question</th><th>Why it matters</th></tr></thead><tbody><tr><td>Can compute and HBM loads run at the same time?</td><td>Reduces Tensor Core starvation.</td></tr><tr><td>Can data be prefetched into local SRAM?</td><td>Hides the latency of the next tile.</td></tr><tr><td>Can network receive overlap with local compute?</td><td>Important in tensor parallelism and MoE.</td></tr><tr><td>Is the copy engine independent of the compute unit?</td><td>Does not waste SMs on data movement.</td></tr></tbody></table><p>Hopper&#39;s TMA, CUDA <code>cp.async</code>, the TPU&#39;s VMEM pipeline, and NCCL communication overlap all come from the same requirement.</p><h2 id="_4-tensor-aware-memory-transfer" tabindex="-1">4. Tensor-Aware Memory Transfer <a class="header-anchor" href="#_4-tensor-aware-memory-transfer" aria-label="Permalink to &quot;4. Tensor-Aware Memory Transfer&quot;">​</a></h2><p>An ordinary DMA moves bytes. What an AI accelerator wants is to move tensors.</p><p>Tensor-aware transfer must have the following properties.</p><table tabindex="0"><thead><tr><th>Need</th><th>Why it matters</th></tr></thead><tbody><tr><td>Layout awareness</td><td>It must understand row-major, column-major, tile layouts, and packed low-bit formats.</td></tr><tr><td>Async scheduling</td><td>Compute and copy must be overlapped.</td></tr><tr><td>Local and remote movement</td><td>Movement inside a chip, between chips, and between nodes must be handled with the same mental model.</td></tr><tr><td>Optional transform</td><td>If transpose, reduce, unpack, and scale can be handled during the copy, compute unit waste is reduced.</td></tr></tbody></table><p>The TMA on the NVIDIA H100, RDMA, GPUDirect, SHARP, and the communication engines of DSAs can all be read in this direction.</p><h3 id="_4-1-process-while-copying" tabindex="-1">4.1 &quot;Process While Copying&quot; <a class="header-anchor" href="#_4-1-process-while-copying" aria-label="Permalink to &quot;4.1 &quot;Process While Copying&quot;&quot;">​</a></h3><p>From the DSA viewpoint, ideal memory movement is not a plain copy. When the tensor arrives at its destination, it must be ready to go straight into compute.</p><p>Possible transforms are the following.</p><table tabindex="0"><thead><tr><th>During movement</th><th>Why useful</th></tr></thead><tbody><tr><td>unpack INT4/FP4</td><td>Connects low-bit storage with high-precision compute.</td></tr><tr><td>transpose / swizzle</td><td>Matches the Tensor Core tile layout.</td></tr><tr><td>scale / zero-point apply</td><td>Converts the quantized tensor into the runtime format.</td></tr><tr><td>reduce / accumulate</td><td>Offloads part of AllReduce or expert aggregation.</td></tr><tr><td>gather / scatter</td><td>Important in MoE routing and paged KV cache.</td></tr></tbody></table><p>The case of MoE systems like DeepSeek V3 using some SMs for communication management shows that without such dedicated hardware, compute silicon can be taken away by communication orchestration.</p><h2 id="_5-scratchpad-instead-of-the-cache-hierarchy" tabindex="-1">5. Scratchpad Instead of the Cache Hierarchy <a class="header-anchor" href="#_5-scratchpad-instead-of-the-cache-hierarchy" aria-label="Permalink to &quot;5. Scratchpad Instead of the Cache Hierarchy&quot;">​</a></h2><p>The CPU cache is tuned for hard-to-predict workloads. But Transformer inference reads very large tensors sequentially, multiplies them, and moves on to the next layer. It is not often that the same weight tensor is reused within a very short time.</p><p>So an ordinary cache policy can be inefficient.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CPU-like cache:</span></span>
<span class="line"><span>  &quot;A value read recently is likely to be used again soon.&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Transformer decode:</span></span>
<span class="line"><span>  &quot;The layer weights just read are not used again until the next token.&quot;</span></span></code></pre></div><p>In this case, the more suitable structure is the scratchpad. A scratchpad is not a cache that the hardware fills and empties on its own; it is fast local memory explicitly managed by software or the compiler.</p><p>The TPU&#39;s VMEM, the GPU&#39;s shared memory/SMEM, and the local SRAM of the Tenstorrent Tensix core can all be viewed from this perspective.</p><h3 id="_5-1-when-the-cache-fits-and-when-it-does-not" tabindex="-1">5.1 When the Cache Fits and When It Does Not <a class="header-anchor" href="#_5-1-when-the-cache-fits-and-when-it-does-not" aria-label="Permalink to &quot;5.1 When the Cache Fits and When It Does Not&quot;">​</a></h3><p>The cache is not always bad. It is useful for data with clear reuse, such as attention tiles, repeated metadata, hot routing tables, and small activations. The problem is when huge tensor streams like LLM weights are put into the cache.</p><table tabindex="0"><thead><tr><th>Data</th><th>Cache usefulness</th><th>Better approach</th></tr></thead><tbody><tr><td>Layer weights in decode</td><td>low</td><td>stream + quantize + prefetch</td></tr><tr><td>Attention tile</td><td>high</td><td>FlashAttention-style tiling</td></tr><tr><td>KV cache page metadata</td><td>medium/high</td><td>cache-friendly layout</td></tr><tr><td>MoE routing table</td><td>high</td><td>local cache or SRAM</td></tr><tr><td>Large intermediate activation</td><td>low</td><td>remove the HBM round-trip with fusion</td></tr></tbody></table><p>So the DSA is closer to the argument that <strong>the predictable parts of the workload should be moved to the scratchpad and compiler/runtime control</strong>, rather than a call to abolish the cache.</p><h3 id="_5-2-the-cost-of-the-scratchpad" tabindex="-1">5.2 The Cost of the Scratchpad <a class="header-anchor" href="#_5-2-the-cost-of-the-scratchpad" aria-label="Permalink to &quot;5.2 The Cost of the Scratchpad&quot;">​</a></h3><p>SRAM is fast but expensive. It consumes a lot of die area and power, so it cannot be grown indefinitely. So scratchpad design is the following trade-off.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>larger scratchpad</span></span>
<span class="line"><span>  -&gt; keep more tiles/weights/KV pages on-chip</span></span>
<span class="line"><span>  -&gt; reduce HBM traffic</span></span>
<span class="line"><span>  -&gt; but compete for area with compute units, IO, yield, and cost</span></span></code></pre></div><p>The core differentiator of a DSA startup is where to land on this trade-off. GPUs strike a balance for broad workloads, while TPUs and inference DSAs can invest more boldly in SRAM and data movement for the more regular AI workload.</p><h2 id="_6-the-kv-cache-is-a-second-model" tabindex="-1">6. The KV Cache Is a Second Model <a class="header-anchor" href="#_6-the-kv-cache-is-a-second-model" aria-label="Permalink to &quot;6. The KV Cache Is a Second Model&quot;">​</a></h2><p>The note uses attention and the KV cache to explain the bottleneck of long-context inference.</p><p>In MHA, the K and V of every head are stored per layer. As the context grows, the KV cache pressures both HBM capacity and bandwidth at the same time.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>KV cache bytes/token</span></span>
<span class="line"><span>  ~= 2(K,V) * layers * heads * head_dim * bytes_per_value</span></span></code></pre></div><p>MQA, GQA, and MLA are all attempts to reduce this term.</p><table tabindex="0"><thead><tr><th>Method</th><th>What it reduces</th><th>Trade-off</th></tr></thead><tbody><tr><td>MQA</td><td>the number of K/V heads</td><td>model quality or an architecture constraint</td></tr><tr><td>GQA</td><td>shrinks the K/V head count at group granularity</td><td>a compromise between MHA and MQA</td></tr><tr><td>MLA</td><td>compresses K/V into a latent representation</td><td>increased projection compute</td></tr><tr><td>KV quantization</td><td>bytes per value</td><td>requires accuracy and kernel support validation</td></tr></tbody></table><p>Restating the Week 3 KV cache content from the hardware perspective:</p><blockquote><p>Long context starts as a KV cache bytes/token problem, not an attention FLOPS problem.</p></blockquote><h3 id="_6-1-computing-kv-cache-capacity" tabindex="-1">6.1 Computing KV Cache Capacity <a class="header-anchor" href="#_6-1-computing-kv-cache-capacity" aria-label="Permalink to &quot;6.1 Computing KV Cache Capacity&quot;">​</a></h3><p>For a decoder-only Transformer, the KV cache per token is approximately the following.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bytes/token = 2 * n_layers * n_kv_heads * head_dim * bytes_per_value</span></span></code></pre></div><p>Here <code>2</code> means K and V. In MHA, <code>n_kv_heads = n_attention_heads</code>, and in GQA/MQA it becomes much smaller.</p><p>For example, a model with 80 layers, 64 KV heads, and head_dim 128 in BF16 is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>2 * 80 * 64 * 128 * 2 bytes = 2,621,440 bytes/token ~= 2.5 MB/token</span></span></code></pre></div><p>If 1,000 concurrent requests have an average context of 4,000 tokens, the KV cache alone is about 10 TB. This is why paged KV cache, prefix caching, chunked prefill, and disaggregation matter in a real serving system.</p><h3 id="_6-2-the-kv-cache-is-also-a-bandwidth-problem" tabindex="-1">6.2 The KV Cache Is Also a Bandwidth Problem <a class="header-anchor" href="#_6-2-the-kv-cache-is-also-a-bandwidth-problem" aria-label="Permalink to &quot;6.2 The KV Cache Is Also a Bandwidth Problem&quot;">​</a></h3><p>Capacity is not the only problem. Decode attention must read the past KV cache for every token. As the context grows, the per-token latency can increase.</p><table tabindex="0"><thead><tr><th>Optimization</th><th>Capacity effect</th><th>Bandwidth effect</th></tr></thead><tbody><tr><td>MQA/GQA</td><td>fewer KV heads</td><td>fewer read bytes</td></tr><tr><td>MLA</td><td>compress into a latent cache</td><td>fewer read bytes, more projection compute</td></tr><tr><td>KV quantization</td><td>fewer bytes/value</td><td>fewer read bytes, requires quality/kernel validation</td></tr><tr><td>PagedAttention</td><td>less fragmentation</td><td>better locality and allocator stability</td></tr><tr><td>Prefix caching</td><td>less duplicate prefill</td><td>shared prefix reuse</td></tr></tbody></table><p>This is why DSA design treats the KV cache as a separate first-class workload. Even if weight streaming alone is fast, if long-context decode is slow, serving quality does not come out.</p><h2 id="_7-scale-out-the-ops-comms-ratio" tabindex="-1">7. Scale-Out: The Ops:Comms Ratio <a class="header-anchor" href="#_7-scale-out-the-ops-comms-ratio" aria-label="Permalink to &quot;7. Scale-Out: The Ops:Comms Ratio&quot;">​</a></h2><p>If the model does not fit on a single accelerator, sharding is needed. At that point, the new bottleneck is the ratio of compute to communication.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ops:comms ratio = accelerator compute throughput / interconnect bandwidth</span></span></code></pre></div><p>Tensor parallelism must exchange activations inside a layer. Expert parallelism must send tokens to the devices where the experts live and gather them back. If this communication is not hidden by compute, the GPU or accelerator sits idle.</p><table tabindex="0"><thead><tr><th>Parallelism</th><th>Main communication</th><th>Practical reading</th></tr></thead><tbody><tr><td>Tensor parallelism</td><td>AllReduce / AllGather / ReduceScatter</td><td>It is advantageous to keep it within NVLink/NVSwitch.</td></tr><tr><td>Pipeline parallelism</td><td>activation transfer</td><td>Relatively easy to cross between nodes.</td></tr><tr><td>Expert parallelism</td><td>AllToAll</td><td>Fabric latency and routing are extremely important.</td></tr><tr><td>Data parallelism</td><td>gradient AllReduce</td><td>Important in training.</td></tr></tbody></table><p>In MoE models, the AllToAll of expert parallelism is especially important. Without communication-dedicated hardware, compute SMs must be spent on communication processing, which is a waste of Tensor Cores.</p><h3 id="_7-1-the-intuition-of-tensor-parallelism" tabindex="-1">7.1 The Intuition of Tensor Parallelism <a class="header-anchor" href="#_7-1-the-intuition-of-tensor-parallelism" aria-label="Permalink to &quot;7.1 The Intuition of Tensor Parallelism&quot;">​</a></h3><p>Tensor parallelism splits the weight matrix across multiple devices. Then the compute and memory each device carries shrink, but communication arises to align activations at the layer boundary.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>benefit:</span></span>
<span class="line"><span>  reduced per-device weight capacity pressure</span></span>
<span class="line"><span>  reduced per-device compute</span></span>
<span class="line"><span></span></span>
<span class="line"><span>cost:</span></span>
<span class="line"><span>  AllReduce / AllGather / ReduceScatter</span></span>
<span class="line"><span>  latency and bandwidth overhead</span></span></code></pre></div><p>In decode, the batch is small, so there is little chance that compute can hide the communication. So raising the TP degree blindly can actually make things slower.</p><h3 id="_7-2-the-intuition-of-expert-parallelism" tabindex="-1">7.2 The Intuition of Expert Parallelism <a class="header-anchor" href="#_7-2-the-intuition-of-expert-parallelism" aria-label="Permalink to &quot;7.2 The Intuition of Expert Parallelism&quot;">​</a></h3><p>In MoE, the selected expert differs per token. If experts are split across devices, tokens must be sent to the devices where the experts live.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>route tokens -&gt; AllToAll -&gt; expert FFN -&gt; AllToAll/aggregate -&gt; continue</span></span></code></pre></div><p>This traffic is more irregular than the AllReduce of dense TP. If the token distribution is uneven, some experts/devices become the bottleneck. So for an MoE DSA, the following capabilities matter beyond simple bandwidth.</p><table tabindex="0"><thead><tr><th>Need</th><th>Reason</th></tr></thead><tbody><tr><td>Low-latency AllToAll</td><td>Token dispatch is reflected directly in decode latency.</td></tr><tr><td>Efficient gather/scatter</td><td>The token order and expert order must keep being swapped.</td></tr><tr><td>Load balancing support</td><td>If traffic piles up on a hot expert, tail latency grows.</td></tr><tr><td>Communication/computation overlap</td><td>The next dispatch must be prepared during the expert FFN.</td></tr></tbody></table><h2 id="_8-what-test-time-compute-scaling-changes" tabindex="-1">8. What Test-Time Compute Scaling Changes <a class="header-anchor" href="#_8-what-test-time-compute-scaling-changes" aria-label="Permalink to &quot;8. What Test-Time Compute Scaling Changes&quot;">​</a></h2><p>The later half of the original deals with the fact that the inference paradigm is not fixed. As ways of spending more compute during inference — reasoning models, search, verifiers, speculative decoding, multi-sample generation — increase, the hardware target changes too.</p><h3 id="_8-1-serial-vs-parallel-test-time-compute" tabindex="-1">8.1 Serial vs Parallel Test-Time Compute <a class="header-anchor" href="#_8-1-serial-vs-parallel-test-time-compute" aria-label="Permalink to &quot;8.1 Serial vs Parallel Test-Time Compute&quot;">​</a></h3><p>Test-time compute grows in two main directions.</p><table tabindex="0"><thead><tr><th>Mode</th><th>Example</th><th>Hardware implication</th></tr></thead><tbody><tr><td>Serial</td><td>long chain-of-thought, multi-step reasoning</td><td>long decode latency, KV cache growth</td></tr><tr><td>Parallel</td><td>sampling multiple candidates, verifier/reranker</td><td>increased batch and throughput, scheduling matters</td></tr></tbody></table><p>Serial scaling pressures per-request latency and KV cache capacity. Parallel scaling can raise hardware utilization by growing the batch, but it also demands more memory and scheduler sophistication.</p><h3 id="_8-2-speculative-decoding-and-the-dsa" tabindex="-1">8.2 Speculative Decoding and the DSA <a class="header-anchor" href="#_8-2-speculative-decoding-and-the-dsa" aria-label="Permalink to &quot;8.2 Speculative Decoding and the DSA&quot;">​</a></h3><p>Speculative decoding has a draft model propose several tokens and the target model verify them at once. If the acceptance rate is high, the number of decode steps of the target model can be reduced.</p><p>From the DSA viewpoint, the following questions arise.</p><table tabindex="0"><thead><tr><th>Question</th><th>Why it matters</th></tr></thead><tbody><tr><td>Should the draft model live on the same accelerator?</td><td>memory capacity and scheduling trade-off</td></tr><tr><td>Is the target verification handled as a prefill-like batch?</td><td>Tensor Core utilization can improve</td></tr><tr><td>How much compute is wasted on rejected tokens?</td><td>a low acceptance rate reduces the gain</td></tr><tr><td>Is KV cache rollback/update fast?</td><td>a serving runtime and memory layout problem</td></tr></tbody></table><p>In other words, speculative decoding is an algorithmic trick, but the real gain depends on how well the hardware and runtime handle the verification batch.</p><h3 id="_8-3-the-bottleneck-in-the-age-of-reasoning-models" tabindex="-1">8.3 The Bottleneck in the Age of Reasoning Models <a class="header-anchor" href="#_8-3-the-bottleneck-in-the-age-of-reasoning-models" aria-label="Permalink to &quot;8.3 The Bottleneck in the Age of Reasoning Models&quot;">​</a></h3><p>When reasoning models produce long answers and many internal tokens, the number of decode tokens grows. In that case, not only single-token latency but also tokens per joule, KV cache retention, and multi-turn cache reuse become important.</p><table tabindex="0"><thead><tr><th>Workload shift</th><th>Hardware pressure</th></tr></thead><tbody><tr><td>Longer outputs</td><td>increased decode bandwidth and energy</td></tr><tr><td>More parallel samples</td><td>pressure on the scheduler, batch packing, and memory capacity</td></tr><tr><td>Verifier/reranker</td><td>heterogeneous model serving needed</td></tr><tr><td>Tool use / agent loops</td><td>increased latency variance and CPU/GPU orchestration</td></tr></tbody></table><p>When designing or buying a DSA, looking only at the current benchmark is dangerous. Depending on whether the future workload is serial reasoning, parallel sampling, or MoE-heavy serving, a different accelerator may turn out to be the good one.</p><h2 id="_9-domain-specific-architecture-cases" tabindex="-1">9. Domain-Specific Architecture Cases <a class="header-anchor" href="#_9-domain-specific-architecture-cases" aria-label="Permalink to &quot;9. Domain-Specific Architecture Cases&quot;">​</a></h2><p>The original contrasts several architectures with the design principles. Here, the summary is based on publicly known features.</p><h3 id="_9-1-tpu" tabindex="-1">9.1 TPU <a class="header-anchor" href="#_9-1-tpu" aria-label="Permalink to &quot;9.1 TPU&quot;">​</a></h3><p>The TPU is a representative DSA centered on matrix multiplication. The MXU systolic array, VMEM scratchpad, HBM, and ICI topology are the core.</p><p>Where the TPU fits the original&#39;s design principles well is the following.</p><table tabindex="0"><thead><tr><th>Principle</th><th>TPU interpretation</th></tr></thead><tbody><tr><td>Low precision</td><td>per-generation lower precision support such as BF16 and INT8</td></tr><tr><td>Async transfer</td><td>HBM -&gt; VMEM -&gt; MXU pipeline</td></tr><tr><td>Scratchpad</td><td>VMEM plays the role of programmer/compiler-controlled local memory</td></tr><tr><td>Scale-out</td><td>ICI torus topology</td></tr><tr><td>Communication-aware</td><td>sharding axis and topology matching matter</td></tr></tbody></table><p>The TPU is strong at regular large matmuls and compiler-managed workloads. In exchange, it may be more constrained than a GPU for irregular kernels, the custom CUDA ecosystem, and dynamic serving features.</p><h3 id="_9-2-tenstorrent" tabindex="-1">9.2 Tenstorrent <a class="header-anchor" href="#_9-2-tenstorrent" aria-label="Permalink to &quot;9.2 Tenstorrent&quot;">​</a></h3><p>The Tenstorrent family of architectures emphasizes many small compute tiles with local SRAM, a NoC, and Ethernet-oriented scale-out. The interesting point in the original is that it separates compute cores from data movement cores.</p><p>Read from the DSA viewpoint, the message is the following.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>It is not enough to place many compute cores;</span></span>
<span class="line"><span>the cores/network that manage data movement must be designed together.</span></span></code></pre></div><p>This approach is likely advantageous for workloads with complex communication patterns, such as MoE, AllToAll, and distributed inference. However, the software stack and compiler maturity determine the real performance.</p><h3 id="_9-3-groq" tabindex="-1">9.3 Groq <a class="header-anchor" href="#_9-3-groq" aria-label="Permalink to &quot;9.3 Groq&quot;">​</a></h3><p>The Groq LPU is known for deterministic execution and an SRAM-centered design. Rather than relying on large HBM bandwidth, it emphasizes compile-time scheduling and predictable latency.</p><p>Workloads where this design is attractive are the following.</p><table tabindex="0"><thead><tr><th>Good fit</th><th>Reason</th></tr></thead><tbody><tr><td>Low-latency single stream</td><td>deterministic scheduling can reduce tail latency.</td></tr><tr><td>Fixed model graph</td><td>compile-time optimization becomes stronger.</td></tr><tr><td>Small/medium model serving</td><td>the benefits of on-chip memory and predictable dataflow are large.</td></tr></tbody></table><p>Conversely, it may be constrained for very large frontier models, dynamic routing, and ecosystem integration.</p><h3 id="_9-4-cerebras" tabindex="-1">9.4 Cerebras <a class="header-anchor" href="#_9-4-cerebras" aria-label="Permalink to &quot;9.4 Cerebras&quot;">​</a></h3><p>The Cerebras WSE goes in the direction of reducing &quot;the cost of leaving the chip&quot; through a wafer-scale chip and a large on-chip SRAM. This can be seen as the case that pushes the original&#39;s memory movement axiom the furthest.</p><p>If the model or working set fits on-chip well, HBM/scale-out traffic can be reduced greatly. But wafer-scale hardware is special in cost, packaging, software stack, and workload fit alike.</p><h3 id="_9-5-is-the-gpu-not-a-dsa" tabindex="-1">9.5 Is the GPU Not a DSA? <a class="header-anchor" href="#_9-5-is-the-gpu-not-a-dsa" aria-label="Permalink to &quot;9.5 Is the GPU Not a DSA?&quot;">​</a></h3><p>Modern GPUs were originally for graphics, but through Tensor Cores, FP8, TMA, NVLink, NVSwitch, NCCL, and the Transformer Engine, they are in fact becoming increasingly specialized for AI workloads.</p><p>The strengths of the GPU are the following.</p><table tabindex="0"><thead><tr><th>Strength</th><th>Why it matters</th></tr></thead><tbody><tr><td>Ecosystem</td><td>PyTorch, CUDA, Triton, vLLM, TensorRT-LLM</td></tr><tr><td>Flexibility</td><td>Responds quickly to new architectures and custom kernels</td></tr><tr><td>Scale-up fabric</td><td>Strong at TP via NVLink/NVSwitch</td></tr><tr><td>Procurement</td><td>Good cloud/on-prem availability</td></tr></tbody></table><p>The weakness is the cost of generality. Because it is tuned for every workload, looking at a specific inference workload alone, a DSA can win on SRAM, communication offload, and deterministic scheduling.</p><h2 id="_10-the-hardware-software-co-design-viewpoint" tabindex="-1">10. The Hardware/Software Co-Design Viewpoint <a class="header-anchor" href="#_10-the-hardware-software-co-design-viewpoint" aria-label="Permalink to &quot;10. The Hardware/Software Co-Design Viewpoint&quot;">​</a></h2><p>If you read a DSA note as only a hardware story, you have read only half of it. The actual inference performance depends on how well the software stack brings out the hardware properties.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Required capability</th></tr></thead><tbody><tr><td>Compiler</td><td>graph fusion, layout transform, tiling, async scheduling</td></tr><tr><td>Runtime</td><td>batching, KV paging, prefill/decode scheduling</td></tr><tr><td>Kernel library</td><td>low-bit GEMM/GEMV, attention, MoE dispatch</td></tr><tr><td>Distributed runtime</td><td>collectives, topology-aware placement, overlap</td></tr><tr><td>Observability</td><td>bandwidth, queueing, memory pressure, tail latency</td></tr></tbody></table><p>For example, even if the hardware supports INT4, if the runtime handles dequantization with a slow kernel, the latency does not go down. Even if the hardware provides a fast interconnect, if the scheduler does not place the TP group to match the topology, the collectives become the bottleneck.</p><h2 id="_11-criteria-for-evaluating-a-domain-specific-accelerator" tabindex="-1">11. Criteria for Evaluating a Domain-Specific Accelerator <a class="header-anchor" href="#_11-criteria-for-evaluating-a-domain-specific-accelerator" aria-label="Permalink to &quot;11. Criteria for Evaluating a Domain-Specific Accelerator&quot;">​</a></h2><p>After reading this note, it is better not to start looking at an accelerator from the peak FLOPS on the spec sheet. The following order is more practical.</p><ol><li>Confirm whether the target workload is prefill, decode, or training.</li><li>Look at the HBM capacity and bandwidth.</li><li>Look at the SRAM/scratchpad capacity and the software control scheme.</li><li>Look at the native support for low precision formats.</li><li>Look at the host-device, device-device, and rack-scale interconnects.</li><li>Look at how well it handles collectives and AllToAll.</li><li>Confirm whether the compiler/runtime ecosystem lowers the actual model graph well.</li></ol><h3 id="_11-1-a-pre-purchase-adoption-questionnaire" tabindex="-1">11.1 A Pre-Purchase/Adoption Questionnaire <a class="header-anchor" href="#_11-1-a-pre-purchase-adoption-questionnaire" aria-label="Permalink to &quot;11.1 A Pre-Purchase/Adoption Questionnaire&quot;">​</a></h3><p>From the actual platform team&#39;s standpoint, the following questions are more direct.</p><table tabindex="0"><thead><tr><th>Area</th><th>Question</th></tr></thead><tbody><tr><td>Model fit</td><td>Do the target model and KV cache fit? Did you include concurrency?</td></tr><tr><td>Decode</td><td>What are the tok/s and p99 latency at batch=1, batch=8, and batch=64?</td></tr><tr><td>Prefill</td><td>How does the TTFT change on long prompts?</td></tr><tr><td>Quantization</td><td>Which formats are native, and which kernel/runtime supports them?</td></tr><tr><td>MoE</td><td>Did you measure AllToAll and expert imbalance?</td></tr><tr><td>Networking</td><td>Do the scale-up and scale-out topologies match the serving parallelism?</td></tr><tr><td>Software</td><td>Of PyTorch/vLLM/TensorRT-LLM/JAX/XLA, which is production-ready?</td></tr><tr><td>Operations</td><td>Are telemetry, failure handling, rolling deploy, and isolation possible?</td></tr><tr><td>Cost</td><td>Did you look at tokens/sec/$, tokens/sec/W, rack power, and cooling together?</td></tr></tbody></table><h3 id="_11-2-points-to-watch-when-reading-a-benchmark" tabindex="-1">11.2 Points to Watch When Reading a Benchmark <a class="header-anchor" href="#_11-2-points-to-watch-when-reading-a-benchmark" aria-label="Permalink to &quot;11.2 Points to Watch When Reading a Benchmark&quot;">​</a></h3><p>DSA vendor benchmarks usually show the workload that fits best. So you must confirm the following.</p><table tabindex="0"><thead><tr><th>Benchmark claim</th><th>Missing question</th></tr></thead><tbody><tr><td>High TOPS/FLOPS</td><td>What about memory bandwidth and utilization?</td></tr><tr><td>High tokens/sec</td><td>What about batch size and the latency SLA?</td></tr><tr><td>Low latency</td><td>What about concurrency and sequence length?</td></tr><tr><td>INT4 speedup</td><td>What about quality and the exact quantization method?</td></tr><tr><td>MoE support</td><td>What about AllToAll p99 and expert imbalance?</td></tr><tr><td>Scale-out result</td><td>What about topology, collective algorithm, and failure domain?</td></tr></tbody></table><h2 id="_12-practical-interpretation-in-this-repository" tabindex="-1">12. Practical Interpretation in This Repository <a class="header-anchor" href="#_12-practical-interpretation-in-this-repository" aria-label="Permalink to &quot;12. Practical Interpretation in This Repository&quot;">​</a></h2><p>After reading this appendix, the Week 2-4 experiments can be interpreted like the following.</p><table tabindex="0"><thead><tr><th>Measurement</th><th>DSA lens</th></tr></thead><tbody><tr><td>Week 2 GEMV/GEMM transition</td><td>The batch shape changes the arithmetic intensity and changes the hardware path.</td></tr><tr><td>Week 2 GPU-Util mismatch</td><td>&quot;Kernel running&quot; and &quot;useful roofline utilization&quot; are different.</td></tr><tr><td>Week 3 KV cache</td><td>The context length pressures memory capacity and bandwidth at the same time.</td></tr><tr><td>Week 4 INT4 projection</td><td>On edge devices, the reduction in weight bytes is likely to lead directly to latency.</td></tr><tr><td>Week 4 bnb slowdown</td><td>A low-bit format alone is not enough; a fused kernel/runtime is needed.</td></tr></tbody></table><p>The core is always the same.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Confirm the workload phase.</span></span>
<span class="line"><span>2. Compute the bytes moved.</span></span>
<span class="line"><span>3. Estimate the arithmetic intensity.</span></span>
<span class="line"><span>4. Compare it with the hardware ratio.</span></span>
<span class="line"><span>5. Confirm the software overhead and kernel path.</span></span>
<span class="line"><span>6. Then choose the optimization.</span></span></code></pre></div><h2 id="_13-repository-connections" tabindex="-1">13. Repository Connections <a class="header-anchor" href="#_13-repository-connections" aria-label="Permalink to &quot;13. Repository Connections&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Repository topic</th><th>Connection</th></tr></thead><tbody><tr><td>Week 2 hardware foundations</td><td>Extends memory hierarchy, Tensor Core, and HBM bandwidth into DSA design principles.</td></tr><tr><td>Week 3 KV cache</td><td>Provides the basis for looking at the KV cache from both capacity and bandwidth.</td></tr><tr><td>Week 4 quantization</td><td>Gives the viewpoint that low precision affects bandwidth, capacity, and silicon area all.</td></tr><tr><td>AI Systems Performance Engineering Chapter 4</td><td>Connects directly to scale-out, RDMA, collectives, and communication overlap.</td></tr></tbody></table><h2 id="_14-check-questions" tabindex="-1">14. Check Questions <a class="header-anchor" href="#_14-check-questions" aria-label="Permalink to &quot;14. Check Questions&quot;">​</a></h2><ol><li>Why does the latency not go down in the decode phase even if the peak FLOPS are high?</li><li>Why is quantization a bandwidth optimization, not just a memory capacity one?</li><li>Why is a general cache hierarchy not always suitable for Transformer inference?</li><li>Why is AllToAll important in MoE inference?</li><li>What item should be confirmed before FLOPS when evaluating a new AI accelerator?</li><li>How do prefill and decode differ from the arithmetic intensity viewpoint?</li><li>Under what conditions is a scratchpad advantageous over a cache?</li><li>How does test-time compute scaling change the design goal of an inference accelerator?</li><li>Why must you confirm the batch size and the latency SLA together when looking at a DSA benchmark?</li><li>Why does low-bit inference fail if either the hardware support or the runtime/kernel support is missing?</li></ol>`,145))])}const v=n(p,[["render",m]]);export{k as __pageData,v as default};
