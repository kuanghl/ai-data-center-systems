import{_ as t,o as a,c as r,a0 as i}from"./chunks/framework.CzVS_xMp.js";const s="/ai-data-center-systems/assets/execution-architecture-map.DBXLd_HK.svg",k=JSON.parse('{"title":"Lecture 4: GPU Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"gpu/pmpp/lec04/index.md","filePath":"gpu/pmpp/lec04/index.md","lastUpdated":1788490169000}'),n={name:"gpu/pmpp/lec04/index.md"};function l(o,e,c,d,p,h){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="lecture-4-gpu-architecture" tabindex="-1">Lecture 4: GPU Architecture <a class="header-anchor" href="#lecture-4-gpu-architecture" aria-label="Permalink to &quot;Lecture 4: GPU Architecture&quot;">​</a></h1><p>Source: <a href="https://www.youtube.com/watch?v=pBQJAwogMoE&amp;list=PLRRuQYjFhpmubuwx-w8X964ofVkW1T8O4&amp;index=4" target="_blank" rel="noreferrer">PMPP 2021 Lecture 4</a></p><h2 id="table-of-contents" tabindex="-1">Table of Contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of Contents&quot;">​</a></h2><ul><li><a href="#goal">Goal</a></li><li><a href="#lecture-overview">Lecture Overview</a></li><li><a href="#execution-architecture-map">Execution Architecture Map</a></li><li><a href="#gpu-as-a-collection-of-sms">GPU as a Collection of SMs</a></li><li><a href="#thread-blocks-are-assigned-to-sms">Thread Blocks Are Assigned to SMs</a></li><li><a href="#why-a-block-must-fit-on-one-sm">Why a Block Must Fit on One SM</a></li><li><a href="#block-level-collaboration">Block-Level Collaboration</a></li><li><a href="#transparent-scalability">Transparent Scalability</a></li><li><a href="#warps-as-the-scheduling-unit">Warps as the Scheduling Unit</a></li><li><a href="#simd-execution">SIMD Execution</a></li><li><a href="#control-divergence">Control Divergence</a></li><li><a href="#latency-hiding">Latency Hiding</a></li><li><a href="#occupancy">Occupancy</a></li><li><a href="#choosing-a-block-size">Choosing a Block Size</a></li><li><a href="#querying-device-properties">Querying Device Properties</a></li><li><a href="#practical-tips-and-notes">Practical Tips and Notes</a></li><li><a href="#lecture-summary">Lecture Summary</a></li><li><a href="#key-terms">Key Terms</a></li><li><a href="#questions">Questions</a></li><li><a href="#answers">Answers</a></li></ul><hr><h2 id="goal" tabindex="-1">Goal <a class="header-anchor" href="#goal" aria-label="Permalink to &quot;Goal&quot;">​</a></h2><p>이번 강의의 목표는 지금까지 작성한 CUDA kernel이 실제 GPU hardware 위에서 어떻게 실행되는지 이해하는 것이다. 2강과 3강에서는 thread, block, grid를 프로그래밍 모델로 사용했다. 4강은 이 추상화가 GPU의 streaming multiprocessor, warp scheduler, SIMD execution, register file, shared memory, global memory 같은 hardware 구조에 어떻게 mapping되는지 설명한다.</p><p>핵심 메시지는 다음과 같다.</p><blockquote><p>CUDA grid는 block 단위로 SM에 배치되고, block 안의 thread는 warp 단위로 scheduling된다. GPU는 개별 operation의 latency를 낮추는 processor가 아니라, 많은 warp를 resident 상태로 두고 long-latency operation 동안 다른 warp를 실행해 throughput을 높이는 processor다.</p></blockquote><p>이 강의는 다음을 다룬다.</p><ul><li>GPU가 여러 streaming multiprocessor로 구성되는 방식</li><li>V100 예시: 80 SMs, SM당 64 FP32 cores</li><li>Thread block이 SM에 배치되는 규칙</li><li>Block 내부 thread가 같은 SM에 있어야 하는 이유</li><li><code>__syncthreads()</code>와 shared memory가 block-local collaboration인 이유</li><li>Block 간 synchronization을 피해야 하는 이유와 deadlock 위험</li><li>Transparent scalability가 가능한 CUDA block 독립성</li><li>Warp가 SM scheduling의 기본 단위인 이유</li><li>SIMD model의 장점과 control divergence의 비용</li><li>GPU가 long latency를 warp-level multithreading으로 숨기는 방식</li><li>Occupancy의 의미와 block size 선택의 hardware 제약</li><li><code>cudaGetDeviceProperties</code>로 device limit을 확인하는 방법</li></ul><hr><h2 id="lecture-overview" tabindex="-1">Lecture Overview <a class="header-anchor" href="#lecture-overview" aria-label="Permalink to &quot;Lecture Overview&quot;">​</a></h2><p>강의는 3강 복습으로 시작한다. 3강에서는 2D grid와 2D block을 사용해 image와 matrix를 처리했다. RGB to grayscale은 output pixel 하나를 thread 하나가 맡는 단순한 2D data parallelism이었다. Image blur는 output pixel 하나가 여러 input pixel을 읽기 때문에 output boundary와 input boundary를 분리해야 했다. Matrix multiplication은 output matrix의 element 하나를 thread 하나가 맡고, dot product loop는 thread 내부에서 sequential하게 수행했다.</p><p>4강의 본론은 &quot;이 thread들이 GPU에서 실제로 어디에 올라가는가?&quot;라는 질문이다. CUDA code에서는 grid, block, thread라는 논리적 hierarchy를 보지만, hardware에서는 SM, core, register, warp scheduler, instruction dispatch unit, memory hierarchy가 이 hierarchy를 실행한다. GPU 전체에는 여러 SM이 있고, 각 SM 안에는 여러 execution core와 control logic, register file, shared memory가 있다. 모든 SM은 global memory에 접근할 수 있다.</p><p>강의는 먼저 block scheduling을 설명한다. Thread block은 쪼개져 여러 SM에 흩어지지 않는다. Block 안의 모든 thread는 같은 SM에 배치되고, 하나의 SM은 여러 block을 동시에 resident 상태로 둘 수 있다. Grid에 block이 너무 많으면 일부 block은 아직 실행되지 않은 채 기다린다. 이 규칙 때문에 block 안의 thread는 synchronization과 shared memory를 통해 협력할 수 있지만, block 간 synchronization은 일반 CUDA kernel 안에서 안전하지 않다.</p><p>그 다음에는 SM 내부로 zoom-in한다. SM에 올라온 thread는 warp라는 scheduling unit으로 묶인다. Warp size는 device-specific이지만 현재까지 CUDA GPU에서는 보통 32 threads다. 같은 warp의 thread는 SIMD 방식으로 같은 instruction을 실행한다. 이 방식은 instruction fetch/decode/control logic의 비용을 여러 core에 나눠 쓰게 해 GPU가 많은 arithmetic unit을 넣을 수 있게 한다. 대신 warp 안의 thread가 서로 다른 branch를 타면 control divergence가 생기고, 일부 lane은 inactive 상태로 낭비된다.</p><p>마지막으로 GPU가 latency를 다루는 방식을 설명한다. CPU는 out-of-order execution, 큰 cache, wide issue 등으로 single-thread latency를 줄이는 방향에 가깝다. GPU는 반대로 많은 thread와 warp를 resident 상태로 두고, 한 warp가 memory access나 multi-cycle arithmetic 때문에 stall되면 다른 ready warp를 실행한다. 이 능력을 높이는 중요한 지표가 occupancy다.</p><hr><h2 id="execution-architecture-map" tabindex="-1">Execution Architecture Map <a class="header-anchor" href="#execution-architecture-map" aria-label="Permalink to &quot;Execution Architecture Map&quot;">​</a></h2><p><img src="'+s+`" alt="CUDA execution architecture map"></p><p>이 그림에서 중요한 경계는 block과 warp다. Block은 SM에 배치되는 resource allocation 단위이자 shared memory와 <code>__syncthreads()</code>의 collaboration scope다. Warp는 SM 내부에서 instruction을 issue하고 latency를 숨기는 scheduling 단위다.</p><hr><h2 id="gpu-as-a-collection-of-sms" tabindex="-1">GPU as a Collection of SMs <a class="header-anchor" href="#gpu-as-a-collection-of-sms" aria-label="Permalink to &quot;GPU as a Collection of SMs&quot;">​</a></h2><p>GPU는 여러 streaming multiprocessor로 구성된다. 보통 줄여서 SM이라고 부른다. 각 SM은 여러 core, control logic, register file, shared memory, cache 같은 hardware resource를 갖고 있다. 모든 SM은 GPU global memory에 접근할 수 있다.</p><table tabindex="0"><thead><tr><th>Level</th><th>Role</th></tr></thead><tbody><tr><td>GPU</td><td>여러 SM과 global memory를 포함하는 device</td></tr><tr><td>SM</td><td>Thread block을 resident 상태로 두고 warp를 scheduling하는 execution unit</td></tr><tr><td>Core</td><td>Arithmetic operation을 실제로 수행하는 execution unit</td></tr><tr><td>Global memory</td><td>모든 SM이 접근할 수 있는 device memory</td></tr><tr><td>Shared memory</td><td>같은 thread block의 thread들이 협력할 때 쓰는 빠른 on-chip memory</td></tr></tbody></table><p>강의의 기준 hardware는 Volta V100이다. V100은 강의 실습 서버에서 사용하는 GPU로 소개된다.</p><table tabindex="0"><thead><tr><th>V100 resource</th><th>Value in lecture</th></tr></thead><tbody><tr><td>SM count</td><td>80</td></tr><tr><td>FP32 cores per SM</td><td>64</td></tr><tr><td>Total FP32 cores</td><td><code>80 * 64 = 5120</code></td></tr><tr><td>Max threads per SM</td><td>2048</td></tr><tr><td>Max blocks per SM</td><td>32</td></tr><tr><td>Max threads per block</td><td>1024</td></tr></tbody></table><p>이 표에서 중요한 점은 core 수와 thread 수가 같지 않다는 것이다. V100의 SM 하나에는 64 FP32 cores가 있지만, 동시에 resident 상태로 둘 수 있는 thread는 2048개다. 즉, GPU는 &quot;한 core에 thread 하나&quot;로 단순히 생각하면 안 된다. SM은 실제로 한 순간에 실행하는 thread보다 훨씬 많은 thread context를 보유하고, warp scheduler가 그중 ready warp를 골라 실행한다.</p><hr><h2 id="thread-blocks-are-assigned-to-sms" tabindex="-1">Thread Blocks Are Assigned to SMs <a class="header-anchor" href="#thread-blocks-are-assigned-to-sms" aria-label="Permalink to &quot;Thread Blocks Are Assigned to SMs&quot;">​</a></h2><p>CUDA grid는 block의 집합이고, block은 thread의 집합이다. Hardware에서 scheduling될 때 기본 규칙은 다음과 같다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>one thread block -&gt; one SM</span></span>
<span class="line"><span>all threads in the same block -&gt; the same SM</span></span>
<span class="line"><span>one SM -&gt; multiple resident thread blocks possible</span></span></code></pre></div><p>Block 내부 thread 일부를 한 SM에 놓고 나머지를 다른 SM에 놓는 방식은 허용되지 않는다. 반대로 하나의 SM이 여러 block을 동시에 resident 상태로 둘 수는 있다. 예를 들어 어떤 SM에 세 개의 thread block이 올라가 있을 수 있다.</p><p>Grid가 GPU 전체에서 동시에 처리할 수 있는 block 수보다 크면 어떻게 되는가? 나머지 block은 논리적으로 대기한다. 먼저 resident 상태로 올라간 block들이 끝나면, 그 자리를 waiting block이 차지한다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>grid</span></span>
<span class="line"><span>  block 0  -&gt; SM 0</span></span>
<span class="line"><span>  block 1  -&gt; SM 1</span></span>
<span class="line"><span>  block 2  -&gt; SM 0</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>  block k  -&gt; waiting until resources are available</span></span></code></pre></div><p>여기서 &quot;assigned to an SM&quot;은 반드시 지금 core에서 instruction을 실행 중이라는 뜻이 아니다. 더 정확히는 해당 block의 thread들이 SM의 execution resource를 예약했다는 뜻이다. 각 thread는 register, thread slot, control metadata 같은 resource를 필요로 한다.</p><hr><h2 id="why-a-block-must-fit-on-one-sm" tabindex="-1">Why a Block Must Fit on One SM <a class="header-anchor" href="#why-a-block-must-fit-on-one-sm" aria-label="Permalink to &quot;Why a Block Must Fit on One SM&quot;">​</a></h2><p>Block은 한 번에 통째로 SM에 배치되어야 한다. 예를 들어 block 하나가 1024 threads이고 어떤 SM에 512 thread slots만 비어 있다면, CUDA runtime이나 hardware scheduler가 block 절반만 먼저 올리고 나머지 절반을 나중에 올리는 식으로 처리하지 않는다.</p><p>이 규칙은 synchronization 때문에 필요하다. Block 안의 thread는 <code>__syncthreads()</code> 같은 barrier synchronization을 사용할 수 있다. 만약 1024-thread block 중 512 threads만 먼저 실행되고 나머지 512 threads는 아직 scheduling되지 않았다면 다음 deadlock이 가능하다.</p><ol><li>먼저 올라간 512 threads가 barrier에 도착한다.</li><li>이 512 threads는 나머지 512 threads가 barrier에 도착하기를 기다린다.</li><li>아직 올라가지 못한 512 threads는 먼저 올라간 512 threads가 끝나 resource를 반납하기를 기다린다.</li><li>양쪽이 서로를 기다리므로 진행이 멈춘다.</li></ol><p>따라서 block을 SM에 배치하려면 그 block의 모든 thread가 필요한 resource를 확보할 수 있어야 한다.</p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p>Thread block은 scheduling과 resource allocation의 atomic한 단위로 생각해야 한다. Block 일부만 먼저 실행하고 나머지를 나중에 실행하는 모델을 가정하면 barrier semantics를 잘못 이해하게 된다.</p></div><hr><h2 id="block-level-collaboration" tabindex="-1">Block-Level Collaboration <a class="header-anchor" href="#block-level-collaboration" aria-label="Permalink to &quot;Block-Level Collaboration&quot;">​</a></h2><p>CUDA가 grid를 block으로 나누는 중요한 이유는 block 내부 thread가 서로 협력할 수 있기 때문이다. 같은 block의 thread는 같은 SM에 배치되므로, hardware는 이들 사이의 collaboration을 비교적 효율적으로 지원할 수 있다.</p><p>대표적인 collaboration mechanism은 두 가지다.</p><table tabindex="0"><thead><tr><th>Mechanism</th><th>Scope</th><th>Purpose</th></tr></thead><tbody><tr><td><code>__syncthreads()</code></td><td>Threads in the same block</td><td>Block 안의 모든 thread가 특정 지점에 도착할 때까지 기다림</td></tr><tr><td>Shared memory</td><td>Threads in the same block</td><td>Block-local data exchange와 reuse</td></tr></tbody></table><p><code>__syncthreads()</code>는 barrier다. Block 안의 모든 thread가 barrier에 도착해야 그 이후 instruction으로 진행할 수 있다. 이 기능은 이후 shared memory tiling, reduction, stencil computation 같은 pattern에서 핵심이 된다.</p><p>Shared memory도 block-local이다. 같은 block에 속한 thread는 shared memory를 통해 intermediate data를 교환할 수 있지만, 다른 block의 thread는 그 shared memory에 접근할 수 없다. 이 제약은 단점처럼 보일 수 있지만, CUDA의 scalability를 가능하게 하는 핵심 설계다.</p><hr><h2 id="transparent-scalability" tabindex="-1">Transparent Scalability <a class="header-anchor" href="#transparent-scalability" aria-label="Permalink to &quot;Transparent Scalability&quot;">​</a></h2><p>CUDA programming model에서 block 간 thread는 일반적으로 서로 synchronize하지 않는다. 이 독립성 덕분에 block은 어떤 순서로 실행되어도 된다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Allowed block execution orders:</span></span>
<span class="line"><span>  block 0, block 1, block 2, ...</span></span>
<span class="line"><span>  block 127, block 126, ...</span></span>
<span class="line"><span>  some blocks in parallel, then remaining blocks later</span></span></code></pre></div><p>이 특성을 transparent scalability라고 부른다. 같은 CUDA kernel이 작은 GPU에서도, 큰 GPU에서도 동작할 수 있다. 작은 GPU는 같은 grid의 block을 더 많이 sequential하게 실행하고, 큰 GPU는 더 많은 block을 동시에 실행한다. Programmer가 code를 바꾸지 않아도 hardware parallelism이 늘어나면 더 많은 block이 병렬로 처리될 수 있다.</p><p>Block 간 synchronization을 kernel 내부에서 억지로 구현하려고 하면 이 모델이 깨진다. 예를 들어 global memory에 flag를 두고 spin loop로 다른 block을 기다리는 코드를 작성하면, 기다리는 대상 block이 아직 실행조차 되지 않았을 수 있다. 그러면 실행 중인 block은 기다리고, waiting block은 SM resource가 비기를 기다리므로 deadlock이 발생할 수 있다.</p><table tabindex="0"><thead><tr><th>Synchronization target</th><th>CUDA kernel 안에서의 의미</th></tr></thead><tbody><tr><td>Threads in same block</td><td><code>__syncthreads()</code>로 가능</td></tr><tr><td>Threads in different blocks</td><td>일반 kernel 내부에서 가정하면 위험</td></tr><tr><td>All blocks in a grid</td><td>Kernel completion을 synchronization point로 사용</td></tr></tbody></table><p>실무적으로는 block 사이에 global synchronization이 필요하면 kernel을 분리한다. 같은 stream에서 kernel을 순차 launch하거나 host/device synchronization으로 이전 kernel의 completion을 보장하면, 그 completion point가 grid-wide synchronization 역할을 한다.</p><hr><h2 id="warps-as-the-scheduling-unit" tabindex="-1">Warps as the Scheduling Unit <a class="header-anchor" href="#warps-as-the-scheduling-unit" aria-label="Permalink to &quot;Warps as the Scheduling Unit&quot;">​</a></h2><p>SM 내부에서 thread는 개별 thread 단위로 scheduling되지 않는다. Thread block은 warp라는 작은 묶음으로 나뉘고, warp가 SM scheduling의 기본 단위가 된다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>thread block</span></span>
<span class="line"><span>  warp 0: threads 0..31</span></span>
<span class="line"><span>  warp 1: threads 32..63</span></span>
<span class="line"><span>  warp 2: threads 64..95</span></span>
<span class="line"><span>  ...</span></span></code></pre></div><p>Warp size는 device-specific property지만, 강의 시점까지 CUDA GPU에서는 일반적으로 32 threads다. 따라서 1024-thread block은 32 warps로 나뉘고, 64-thread block은 2 warps로 나뉜다.</p><p>Warp는 &quot;같이 움직이는 thread 묶음&quot;이다. 같은 warp의 thread들은 같은 instruction stream을 공유한다. SM의 warp scheduler는 resident warp들 중 ready 상태인 warp를 골라 instruction을 issue한다. 어떤 warp가 memory access나 long-latency operation 때문에 기다려야 하면 scheduler는 다른 ready warp를 고른다.</p><p>Block과 warp의 차이를 구분하는 것이 중요하다.</p><table tabindex="0"><thead><tr><th>Unit</th><th>Meaning</th></tr></thead><tbody><tr><td>Block</td><td>Programmer가 지정하는 collaboration scope</td></tr><tr><td>Warp</td><td>Hardware scheduling과 SIMD execution의 기본 단위</td></tr><tr><td>Thread</td><td>Logical work item, register context를 갖는 execution context</td></tr></tbody></table><hr><h2 id="simd-execution" tabindex="-1">SIMD Execution <a class="header-anchor" href="#simd-execution" aria-label="Permalink to &quot;SIMD Execution&quot;">​</a></h2><p>같은 warp의 thread는 성능 모델상 SIMD 또는 SIMT model로 이해할 수 있다. SIMD는 single instruction, multiple data의 약자다. 하나의 instruction을 fetch/decode/dispatch하고, warp 안의 여러 thread가 각자의 data에 대해 같은 operation을 수행한다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>one instruction</span></span>
<span class="line"><span>  -&gt; thread 0 uses data[0]</span></span>
<span class="line"><span>  -&gt; thread 1 uses data[1]</span></span>
<span class="line"><span>  -&gt; thread 2 uses data[2]</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>  -&gt; thread 31 uses data[31]</span></span></code></pre></div><p>SIMD의 장점은 control overhead를 amortize하는 것이다. CPU처럼 thread마다 독립적인 instruction fetch/decode/control logic을 많이 두면 hardware budget이 control logic에 많이 쓰인다. GPU는 같은 instruction을 여러 lane에 dispatch함으로써 instruction fetch와 decode unit의 비용을 여러 execution unit에 나눠 쓴다. 그 결과 더 많은 silicon area를 arithmetic unit에 투자할 수 있다.</p><p>강의에서는 V100 SM 구조를 예로 든다. V100의 SM 하나에는 64 FP32 cores가 있고, 이것이 4개의 processing block으로 나뉜다. 각 processing block은 16 FP32 cores와 instruction dispatch unit, register file 등을 갖는다. Warp size는 32이므로, 하나의 warp가 16-core processing block에서 여러 cycle에 걸쳐 실행될 수 있다.</p><table tabindex="0"><thead><tr><th>V100 SM component</th><th>Lecture framing</th></tr></thead><tbody><tr><td>64 FP32 cores</td><td>Floating-point operation을 수행</td></tr><tr><td>64 INT32 cores</td><td>Integer operation을 수행</td></tr><tr><td>4 processing blocks</td><td>SM 내부 execution cluster</td></tr><tr><td>16 FP32 cores per processing block</td><td>하나의 dispatch/control unit이 여러 core를 제어</td></tr><tr><td>Warp scheduler</td><td>Ready warp를 선택</td></tr><tr><td>L0 instruction cache</td><td>Processing block에 가까운 instruction cache</td></tr><tr><td>L1 instruction cache/data cache/shared memory</td><td>SM 내부에서 공유되는 on-chip resource</td></tr></tbody></table><p>이 구조는 GPU architecture마다 바뀐다. 강의는 Volta V100을 기준으로 설명하지만, Pascal이나 이후 architecture는 processing block 구성과 core 배치가 다를 수 있다. Volta 이후에는 independent thread scheduling 같은 세부 변화도 있지만, 성능을 해석할 때 변하지 않는 핵심은 &quot;warp 단위 issue/scheduling&quot;과 &quot;control logic을 여러 lane에 나눠 쓰는 SIMD-style execution&quot;이다.</p><hr><h2 id="control-divergence" tabindex="-1">Control Divergence <a class="header-anchor" href="#control-divergence" aria-label="Permalink to &quot;Control Divergence&quot;">​</a></h2><p>SIMD execution의 대표적인 비용은 control divergence다. 같은 warp의 thread는 같은 instruction을 실행해야 하는데, branch 때문에 일부 thread는 <code>then</code> path를, 다른 thread는 <code>else</code> path를 원할 수 있다.</p><p>예를 들어 다음 code를 보자.</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (threadIdx.x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 24</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* code A */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* code B */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* code C */</span></span></code></pre></div><p>첫 번째 warp가 <code>threadIdx.x = 0..31</code>을 포함한다고 하면, threads 0..23은 <code>code A</code>를 실행하고 threads 24..31은 <code>code B</code>를 실행해야 한다. 하지만 warp는 같은 instruction을 함께 실행해야 한다. 따라서 hardware는 대략 다음처럼 처리한다.</p><ol><li>Warp 전체가 branch condition을 평가한다.</li><li><code>code A</code> path를 실행한다. 이때 threads 0..23만 active이고, threads 24..31은 inactive다.</li><li><code>code B</code> path를 실행한다. 이때 threads 24..31만 active이고, threads 0..23은 inactive다.</li><li>Branch 이후 reconverge해서 <code>code C</code>를 함께 실행한다.</li></ol><p>Inactive thread가 있는 동안 해당 lane은 다른 유용한 일을 하지 못한다. 이것이 SIMD efficiency를 낮춘다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SIMD efficiency = active lanes / total lanes</span></span></code></pre></div><p>위 예제에서 <code>code A</code>를 실행할 때는 24/32 lanes만 active이고, <code>code B</code>를 실행할 때는 8/32 lanes만 active다. Branch path가 많아질수록, 또는 각 path의 instruction 수가 길어질수록 낭비가 커진다.</p><p>Loop도 divergence를 만든다. 각 thread의 loop bound가 data-dependent하면 같은 warp 안에서도 thread마다 iteration 수가 다를 수 있다.</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> A</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[threadIdx.x];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">i) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* code A */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>어떤 thread는 4 iterations만 수행하고, 다른 thread는 1000 iterations를 수행한다면, 짧게 끝난 thread들은 warp의 나머지 thread가 loop를 끝낼 때까지 inactive 상태로 묶인다. 강의에서는 이런 상황을 &quot;한 thread가 다른 thread들을 hostage로 잡는&quot; 형태로 설명한다.</p><p>Control divergence를 줄이는 한 가지 실무 전략은 같은 warp에 비슷한 control path를 타는 data를 모으는 것이다. 예를 들어 loop bound가 data value에 의해 결정된다면, 가능할 때 data를 sort해서 비슷한 <code>n</code> 값을 갖는 thread들이 같은 warp에 배치되도록 만들 수 있다. 이것은 compiler가 자동으로 해결하기 어렵고, 대개 programmer나 algorithm design이 책임져야 하는 문제다.</p><hr><h2 id="latency-hiding" tabindex="-1">Latency Hiding <a class="header-anchor" href="#latency-hiding" aria-label="Permalink to &quot;Latency Hiding&quot;">​</a></h2><p>GPU는 long-latency operation을 없애기보다 숨긴다. 한 warp가 memory load miss, DRAM access, multi-cycle arithmetic, dependency chain 같은 이유로 다음 instruction을 실행할 수 없으면, scheduler는 그 warp를 core에서 잠시 빼고 다른 ready warp를 실행한다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>warp 0 executes</span></span>
<span class="line"><span>  -&gt; long-latency memory access</span></span>
<span class="line"><span>  -&gt; warp 0 waits</span></span>
<span class="line"><span>warp scheduler selects warp 1</span></span>
<span class="line"><span>warp 1 executes</span></span>
<span class="line"><span>  -&gt; long-latency operation</span></span>
<span class="line"><span>  -&gt; warp 1 waits</span></span>
<span class="line"><span>warp scheduler selects warp 2</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>warp 0 becomes ready again</span></span>
<span class="line"><span>  -&gt; warp 0 resumes</span></span></code></pre></div><p>이것은 CPU의 context switch와 다르다. GPU에서 resident warp의 registers와 program counter는 여전히 SM에 남아 있다. Warp를 바꾼다고 해서 register context를 memory에 저장하고 다른 thread의 context를 memory에서 복원하는 heavy context switch를 수행하는 것이 아니다. 이미 resident 상태인 여러 warp 중 ready warp를 선택하는 hardware multithreading에 가깝다.</p><p>CPU와 GPU의 latency 전략은 다르다.</p><table tabindex="0"><thead><tr><th>Processor style</th><th>Main goal</th><th>Typical mechanisms</th></tr></thead><tbody><tr><td>CPU</td><td>Low latency for individual thread</td><td>Out-of-order execution, large cache, branch prediction, wide issue</td></tr><tr><td>GPU</td><td>High throughput for many data-parallel operations</td><td>Many cores, many resident warps, warp scheduling, latency hiding</td></tr></tbody></table><p>따라서 &quot;GPU가 CPU보다 빠르다&quot;는 문장은 정확하지 않다. 단일 element를 더하는 latency만 보면 CPU가 더 빠를 수 있다. GPU가 강한 영역은 millions of elements처럼 massive data parallel workload를 높은 throughput으로 처리하는 경우다.</p><hr><h2 id="occupancy" tabindex="-1">Occupancy <a class="header-anchor" href="#occupancy" aria-label="Permalink to &quot;Occupancy&quot;">​</a></h2><p>Occupancy는 SM에 active 또는 resident 상태로 올라간 warp/thread 수가 hardware가 허용하는 최대치에 비해 얼마나 되는지를 나타내는 비율이다. 강의에서는 active를 &quot;실제로 이 순간 core에서 실행 중&quot;이 아니라 &quot;SM에 assigned/resident&quot;라는 의미로 사용한다.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>occupancy = active resident warps or threads / maximum resident warps or threads</span></span></code></pre></div><p>V100 예시에서 SM은 최대 2048 threads를 resident 상태로 둘 수 있다. 어떤 kernel configuration이나 resource 사용량 때문에 SM에 1024 threads만 resident로 올라갈 수 있다면 occupancy는 50%다.</p><p>높은 occupancy가 보통 유리한 이유는 latency hiding 때문이다. Resident warp가 많을수록 한 warp가 stall될 때 scheduler가 선택할 ready warp를 찾을 확률이 높아진다. 하지만 강의에서는 항상 occupancy가 높을수록 성능이 좋은 것은 아니라고도 말한다. 일부 kernel에서는 lower occupancy가 더 나을 수 있다. 그래도 일반적인 출발점은 occupancy를 충분히 확보하는 것이다.</p><p>Occupancy를 제한하는 요인은 여러 가지다.</p><table tabindex="0"><thead><tr><th>Constraint</th><th>How it limits occupancy</th></tr></thead><tbody><tr><td>Max threads per SM</td><td>SM에 resident로 둘 수 있는 thread 수의 상한</td></tr><tr><td>Max blocks per SM</td><td>작은 block을 너무 많이 필요로 할 때 block count가 먼저 limit이 됨</td></tr><tr><td>Max threads per block</td><td>block 하나가 가질 수 있는 thread 수의 상한</td></tr><tr><td>Registers per SM</td><td>Thread당 register 사용량이 많으면 resident thread 수가 줄어듦</td></tr><tr><td>Shared memory per SM</td><td>Block당 shared memory 사용량이 많으면 resident block 수가 줄어듦</td></tr></tbody></table><hr><h2 id="choosing-a-block-size" tabindex="-1">Choosing a Block Size <a class="header-anchor" href="#choosing-a-block-size" aria-label="Permalink to &quot;Choosing a Block Size&quot;">​</a></h2><p>Block size는 occupancy에 직접 영향을 준다. 강의의 V100 예제를 그대로 정리하면 다음과 같다.</p><p>Assume:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>max threads per SM = 2048</span></span>
<span class="line"><span>max blocks per SM  = 32</span></span>
<span class="line"><span>max threads per block = 1024</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Threads per block</th><th>Blocks needed for 2048 threads</th><th>Hardware result</th><th>Occupancy implication</th></tr></thead><tbody><tr><td>256</td><td>8 blocks</td><td>8 &lt;= 32, fits cleanly</td><td>Full occupancy possible</td></tr><tr><td>32</td><td>64 blocks</td><td>64 &gt; 32, block limit reached</td><td>Only <code>32 * 32 = 1024</code> threads, 50% occupancy</td></tr><tr><td>768</td><td>2 full blocks + 512 leftover threads</td><td>Cannot schedule a partial 768-thread block</td><td>1536 resident threads, leftover capacity wasted</td></tr></tbody></table><p>이 예제는 block size를 작게 잡는 것이 항상 좋은 선택이 아님을 보여준다. 32 threads per block은 warp 하나라서 단순해 보이지만, V100에서 full 2048 resident threads를 채우려면 64 blocks가 필요하다. 그런데 SM당 block 수 상한이 32이므로 1024 threads에서 막힌다.</p><p>반대로 768 threads per block은 block 수 상한에는 걸리지 않지만, 2048이 768로 나누어떨어지지 않는다. 두 block을 올리면 1536 threads가 resident가 되고, 남은 512 thread slots는 768-thread block 하나를 더 올리기에는 부족하다. Block은 partial하게 올라갈 수 없기 때문이다.</p><p>실무적으로 block size를 고를 때는 다음 조건을 함께 본다.</p><ul><li>Warp size의 배수인가?</li><li>Target GPU의 max threads per SM을 잘 나눌 수 있는가?</li><li>Max blocks per SM에 너무 빨리 걸리지 않는가?</li><li>Kernel의 register 사용량이 resident thread 수를 줄이지 않는가?</li><li>Block당 shared memory 사용량이 resident block 수를 줄이지 않는가?</li><li>Memory access pattern과 coalescing에 불리하지 않은가?</li></ul><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>초반 CUDA code에서는 128, 256, 512 threads per block을 먼저 실험하는 경우가 많다. 하지만 최종 선택은 target GPU의 occupancy, register pressure, shared memory usage, memory access pattern을 함께 보고 benchmark로 확인해야 한다.</p></div><hr><h2 id="querying-device-properties" tabindex="-1">Querying Device Properties <a class="header-anchor" href="#querying-device-properties" aria-label="Permalink to &quot;Querying Device Properties&quot;">​</a></h2><p>CUDA는 device property를 조회하는 API를 제공한다. 강의에서는 <code>cudaGetDeviceProperties</code>를 언급한다. 이 API를 사용하면 현재 GPU의 max threads per block, max threads per multiprocessor, SM 수 같은 정보를 확인할 수 있다.</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cudaDeviceProp prop;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cudaGetDeviceProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, prop.name);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;multiProcessorCount: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, prop.multiProcessorCount);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxThreadsPerBlock: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, prop.maxThreadsPerBlock);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;maxThreadsPerMultiProcessor: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">       prop.maxThreadsPerMultiProcessor);</span></span></code></pre></div><p>이 값들은 architecture마다 다르다. 따라서 특정 강의 slide의 V100 수치를 절대 규칙으로 외우기보다, target GPU에서 직접 property를 확인하는 습관이 필요하다.</p><hr><h2 id="practical-tips-and-notes" tabindex="-1">Practical Tips and Notes <a class="header-anchor" href="#practical-tips-and-notes" aria-label="Permalink to &quot;Practical Tips and Notes&quot;">​</a></h2><h3 id="treat-blocks-as-independent-work-units" tabindex="-1">Treat Blocks as Independent Work Units <a class="header-anchor" href="#treat-blocks-as-independent-work-units" aria-label="Permalink to &quot;Treat Blocks as Independent Work Units&quot;">​</a></h3><p>CUDA kernel을 설계할 때 block은 독립적으로 실행 가능한 work unit이어야 한다. Block 간에 실행 순서, 동시 실행 여부, 같은 SM 배치 여부를 가정하면 transparent scalability가 깨지고 deadlock 위험이 생긴다.</p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p>Kernel 내부에서 global memory flag와 spin loop로 block 간 barrier를 직접 만들지 마라. 일부 block이 아직 scheduling되지 않은 상태라면 실행 중인 block과 waiting block이 서로를 기다릴 수 있다.</p></div><h3 id="separate-block-synchronization-from-warp-reconvergence" tabindex="-1">Separate Block Synchronization from Warp Reconvergence <a class="header-anchor" href="#separate-block-synchronization-from-warp-reconvergence" aria-label="Permalink to &quot;Separate Block Synchronization from Warp Reconvergence&quot;">​</a></h3><p><code>__syncthreads()</code>는 block-level barrier다. Control divergence 뒤의 reconvergence는 warp 내부 execution control이다. 둘은 서로 다른 개념이다.</p><table tabindex="0"><thead><tr><th>Concept</th><th>Scope</th><th>Trigger</th></tr></thead><tbody><tr><td>Warp reconvergence</td><td>Threads in same warp</td><td>Divergent branch/loop 이후</td></tr><tr><td><code>__syncthreads()</code></td><td>Threads in same block</td><td>Programmer가 명시적으로 호출</td></tr><tr><td>Kernel boundary</td><td>All blocks in grid</td><td>Kernel launch completion</td></tr></tbody></table><p>Warp 하나가 loop divergence 때문에 늦어지는 것과, block 안의 모든 warp가 <code>__syncthreads()</code>에서 기다리는 것은 다른 문제다. 성능 분석에서 이 둘을 구분해야 한다.</p><h3 id="watch-divergence-at-warp-granularity" tabindex="-1">Watch Divergence at Warp Granularity <a class="header-anchor" href="#watch-divergence-at-warp-granularity" aria-label="Permalink to &quot;Watch Divergence at Warp Granularity&quot;">​</a></h3><p>Branch가 있다는 사실 자체보다 같은 warp 안에서 branch direction이 갈리는지가 중요하다. 모든 thread가 같은 branch를 타면 divergence 비용은 작다. Warp 안에서 half는 <code>then</code>, half는 <code>else</code>로 갈리면 두 path를 모두 실행하면서 inactive lane이 생긴다.</p><h3 id="use-occupancy-as-a-diagnostic-not-a-goal-by-itself" tabindex="-1">Use Occupancy as a Diagnostic, Not a Goal by Itself <a class="header-anchor" href="#use-occupancy-as-a-diagnostic-not-a-goal-by-itself" aria-label="Permalink to &quot;Use Occupancy as a Diagnostic, Not a Goal by Itself&quot;">​</a></h3><p>Occupancy는 latency hiding 능력을 설명하는 중요한 지표지만 최종 성능 지표는 아니다. Occupancy를 높이려고 register를 지나치게 줄이면 spilling이 생길 수 있고, shared memory tiling을 줄이면 memory traffic이 늘 수 있다. 좋은 kernel은 occupancy, memory bandwidth, arithmetic intensity, instruction mix를 함께 본다.</p><h3 id="block-size-selection-checklist" tabindex="-1">Block Size Selection Checklist <a class="header-anchor" href="#block-size-selection-checklist" aria-label="Permalink to &quot;Block Size Selection Checklist&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Check</th><th>Why it matters</th></tr></thead><tbody><tr><td>Multiple of 32</td><td>Warp를 partial하게 낭비하지 않기 위한 기본 조건</td></tr><tr><td>128/256/512 baseline</td><td>여러 GPU에서 실험하기 쉬운 일반적인 시작점</td></tr><tr><td>Max blocks per SM</td><td>너무 작은 block은 block count limit에 걸릴 수 있음</td></tr><tr><td>Register usage</td><td>Thread당 register가 많으면 occupancy가 제한됨</td></tr><tr><td>Shared memory per block</td><td>Block당 shared memory가 크면 resident block 수가 줄어듦</td></tr><tr><td>Benchmark</td><td>Occupancy만으로는 실제 runtime을 예측할 수 없음</td></tr></tbody></table><h3 id="quick-reference" tabindex="-1">Quick Reference <a class="header-anchor" href="#quick-reference" aria-label="Permalink to &quot;Quick Reference&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Symptom</th><th>First check</th></tr></thead><tbody><tr><td>Kernel hangs</td><td>Cross-block synchronization or spin waiting</td></tr><tr><td>Good occupancy but poor speed</td><td>Memory coalescing, divergence, arithmetic intensity</td></tr><tr><td>Low occupancy</td><td>Block size, register count, shared memory per block</td></tr><tr><td>Branch-heavy kernel is slow</td><td>Warp-level control divergence</td></tr><tr><td>Small block size underperforms</td><td>Max blocks per SM limiting resident threads</td></tr><tr><td>High register kernel underperforms</td><td>Register pressure or spilling</td></tr></tbody></table><hr><h2 id="lecture-summary" tabindex="-1">Lecture Summary <a class="header-anchor" href="#lecture-summary" aria-label="Permalink to &quot;Lecture Summary&quot;">​</a></h2><p>이번 강의는 CUDA programming model과 GPU architecture를 연결했다. CUDA grid는 block으로 구성되고, block은 SM에 배치된다. 같은 block의 thread는 같은 SM에 있어야 하므로 <code>__syncthreads()</code>와 shared memory 같은 block-local collaboration이 가능하다. 반대로 block 간에는 synchronization을 가정하지 않아야 한다. 이 독립성이 있어야 같은 kernel이 작은 GPU와 큰 GPU에서 모두 실행되는 transparent scalability가 가능하다.</p><p>SM 내부에서는 thread가 warp 단위로 scheduling된다. Warp는 보통 32 threads이고, 같은 warp의 thread는 SIMD 방식으로 같은 instruction을 실행한다. SIMD는 instruction fetch/decode/control logic 비용을 여러 execution lane에 나눠 쓰게 해 throughput-oriented GPU architecture를 가능하게 한다. 하지만 branch나 data-dependent loop가 warp 내부에서 갈라지면 control divergence가 발생하고, inactive lane 때문에 SIMD efficiency가 낮아진다.</p><p>GPU는 CPU처럼 single-thread latency를 낮추는 데 집중하지 않는다. 대신 많은 warp를 SM에 resident 상태로 두고, 한 warp가 long-latency operation을 기다릴 때 다른 ready warp를 실행한다. 이 latency hiding이 GPU throughput의 핵심이다. Occupancy는 SM에 resident한 warp/thread 수가 최대 가능치에 비해 얼마나 되는지를 나타내며, block size, register usage, shared memory usage, hardware limit에 의해 결정된다.</p><hr><h2 id="key-terms" tabindex="-1">Key Terms <a class="header-anchor" href="#key-terms" aria-label="Permalink to &quot;Key Terms&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Term</th><th>Meaning</th></tr></thead><tbody><tr><td>Streaming multiprocessor</td><td>CUDA thread block을 실행하는 GPU의 주요 execution unit</td></tr><tr><td>SM</td><td>Streaming multiprocessor의 약어</td></tr><tr><td>Core</td><td>Arithmetic instruction을 수행하는 execution unit</td></tr><tr><td>Global memory</td><td>모든 SM이 접근할 수 있는 GPU device memory</td></tr><tr><td>Shared memory</td><td>같은 block의 thread들이 공유하는 SM-local memory</td></tr><tr><td>Thread block</td><td>같은 SM에 배치되고 협력할 수 있는 CUDA thread group</td></tr><tr><td>Resident thread</td><td>SM resource를 확보해 active 상태로 올라가 있는 thread</td></tr><tr><td>Barrier synchronization</td><td>여러 thread가 특정 지점에 모두 도착할 때까지 기다리는 synchronization</td></tr><tr><td><code>__syncthreads()</code></td><td>CUDA block-level barrier primitive</td></tr><tr><td>Transparent scalability</td><td>같은 code가 hardware parallelism 크기에 따라 block을 더 병렬 또는 순차적으로 실행할 수 있는 특성</td></tr><tr><td>Warp</td><td>SM scheduling의 기본 단위, 보통 32 threads</td></tr><tr><td>SIMD</td><td>Single instruction, multiple data execution model</td></tr><tr><td>Control divergence</td><td>같은 warp의 thread들이 서로 다른 control path를 택하는 상황</td></tr><tr><td>SIMD efficiency</td><td>SIMD execution 중 active lane 비율</td></tr><tr><td>Reconvergence</td><td>Divergent path 이후 warp thread들이 다시 같은 instruction stream으로 합류하는 것</td></tr><tr><td>Latency hiding</td><td>한 warp가 stall될 때 다른 ready warp를 실행해 pipeline idle을 줄이는 기법</td></tr><tr><td>Occupancy</td><td>SM에 resident한 warp/thread 수와 hardware maximum의 비율</td></tr><tr><td>Register pressure</td><td>Thread당 register 사용량이 많아 occupancy나 spilling에 영향을 주는 상태</td></tr><tr><td><code>cudaGetDeviceProperties</code></td><td>CUDA device capability와 limit을 조회하는 API</td></tr></tbody></table><hr><h2 id="questions" tabindex="-1">Questions <a class="header-anchor" href="#questions" aria-label="Permalink to &quot;Questions&quot;">​</a></h2><ol><li>GPU에서 SM은 어떤 역할을 하는가?</li><li>V100 예시에서 SM 수와 SM당 FP32 core 수는 각각 얼마인가?</li><li>CUDA thread block은 hardware에서 어떤 단위로 SM에 배치되는가?</li><li>Block 안의 thread 일부만 먼저 SM에 올릴 수 없는 이유는 무엇인가?</li><li><code>__syncthreads()</code>는 어떤 scope의 synchronization인가?</li><li>Shared memory는 어떤 thread들이 공유할 수 있는가?</li><li>Block 간 synchronization을 kernel 내부에서 직접 구현하면 왜 deadlock 위험이 있는가?</li><li>Transparent scalability는 무엇을 의미하는가?</li><li>Warp는 무엇이며 일반적인 warp size는 얼마인가?</li><li>SIMD execution의 hardware상 장점은 무엇인가?</li><li>Control divergence는 언제 발생하는가?</li><li>Divergent <code>if-else</code>에서 inactive thread는 왜 다른 일을 할 수 없는가?</li><li>Data-dependent loop bound가 warp divergence를 만들 수 있는 이유는 무엇인가?</li><li>GPU의 latency hiding은 어떤 방식으로 동작하는가?</li><li>GPU warp switching과 CPU context switching의 차이는 무엇인가?</li><li>Occupancy는 어떻게 정의되는가?</li><li>V100에서 32 threads per block이 occupancy를 제한할 수 있는 이유는 무엇인가?</li><li>768 threads per block 예제에서 512 thread slots가 남아도 더 배치하지 못하는 이유는 무엇인가?</li><li>Register usage가 occupancy를 낮출 수 있는 이유는 무엇인가?</li><li><code>cudaGetDeviceProperties</code>로 확인할 수 있는 정보에는 무엇이 있는가?</li></ol><hr><h2 id="answers" tabindex="-1">Answers <a class="header-anchor" href="#answers" aria-label="Permalink to &quot;Answers&quot;">​</a></h2><ol><li>SM은 thread block을 resident 상태로 두고, block 내부 thread를 warp 단위로 scheduling해 실행하는 GPU의 주요 execution unit이다.</li><li>강의의 V100 예시에서는 80 SMs, SM당 64 FP32 cores다.</li><li>Thread block 전체가 하나의 SM에 배치된다.</li><li>일부 thread만 실행되면 <code>__syncthreads()</code> 같은 barrier에서 먼저 실행된 thread가 아직 scheduling되지 않은 thread를 기다리고, scheduling되지 않은 thread는 resource가 비기를 기다리는 deadlock이 가능하기 때문이다.</li><li>같은 block 안의 thread들을 대상으로 하는 block-level synchronization이다.</li><li>같은 block에 속하고 같은 SM에 배치된 thread들이 shared memory를 공유할 수 있다.</li><li>기다리는 대상 block이 아직 SM에 scheduling되지 않았을 수 있고, 실행 중인 block이 끝나지 않으면 그 block은 시작할 수 없어 서로 기다리는 deadlock이 생길 수 있다.</li><li>Block이 서로 독립적이어서 같은 kernel이 작은 GPU에서는 더 순차적으로, 큰 GPU에서는 더 병렬로 실행될 수 있는 특성이다.</li><li>Warp는 SM scheduling과 SIMD execution의 기본 thread 묶음이며, 일반적으로 32 threads다.</li><li>하나의 instruction fetch/decode/dispatch logic을 여러 execution lane이 공유해 control overhead를 줄이고 arithmetic unit을 더 많이 둘 수 있다.</li><li>같은 warp의 thread들이 branch나 loop에서 서로 다른 execution path 또는 다른 iteration 수를 요구할 때 발생한다.</li><li>같은 warp의 thread는 같은 instruction을 실행해야 하므로, 현재 path에 속하지 않은 thread는 다른 instruction을 실행하지 못하고 inactive 상태가 된다.</li><li>Thread마다 loop bound가 다르면 일부 thread는 먼저 loop를 끝내지만, 같은 warp의 나머지 thread가 loop를 계속 실행하는 동안 같이 묶여 있어야 하기 때문이다.</li><li>한 warp가 long-latency operation으로 stall되면 scheduler가 다른 ready warp를 선택해 pipeline이 idle 상태로 남지 않게 한다.</li><li>GPU에서는 resident warp의 register와 program counter가 SM에 남아 있고 scheduler가 ready warp를 고른다. CPU context switching처럼 register context를 memory에 저장하고 복원하는 heavy operation이 아니다.</li><li>SM에 resident한 active warp 또는 thread 수를 hardware가 허용하는 최대 resident warp/thread 수로 나눈 비율이다.</li><li>V100에서 max threads per SM은 2048이고 max blocks per SM은 32다. 32-thread block으로 2048 threads를 채우려면 64 blocks가 필요하지만 SM은 32 blocks까지만 지원하므로 1024 threads에서 제한된다.</li><li>Block은 partial하게 배치될 수 없다. 768-thread block을 하나 더 올리려면 768 slots가 필요하므로 512 slots만으로는 부족하다.</li><li>SM의 register file은 유한하다. Thread당 register 수가 많으면 더 적은 thread만 resident 상태로 둘 수 있어 occupancy가 낮아진다.</li><li>GPU 이름, SM 수, max threads per block, max threads per multiprocessor, block/grid dimension limit 등 device capability와 resource limit을 확인할 수 있다.</li></ol>`,152)])])}const b=t(n,[["render",l]]);export{k as __pageData,b as default};
